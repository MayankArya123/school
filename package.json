{
  "name": "dashboard",
  "version": "1.0.0",
  "engines": {
    "node": "14.x"
  },
  "description": "\"school Dashboard\"",
  "main": "server.js",
  "scripts": {
    "test": "node server",
    "build": "cd client && npm run check",
    "install-client": "cd client && npm install",
    "server": "nodemon server",
    "start1": "node server.js",
    "client": "cd client && npm run start",
    "dev": "concurrently \"npm run server\"  \"npm run client\"",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "author": "mayank",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.11",
    "nodemon": "^2.0.7",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
